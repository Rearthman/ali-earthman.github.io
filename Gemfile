source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# Minimal Mistakes uses 'remote_theme', which is supported by github-pages gem

# When building locally:
#   bundle install
#   bundle exec jekyll serve
